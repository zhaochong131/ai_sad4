const actionAdapter = require('n3h-action-adapter')
const stepAdapter = require('n3h-step-adapter')
const reactorAdapter = require('n3h-reactor-adapter')
const mapperAdapter = require('n3h-mapper-adapter')

module.exports = {
  action: actionAdapter,
  step: stepAdapter,
  reactorAdapter,
  mapperAdapter
}
